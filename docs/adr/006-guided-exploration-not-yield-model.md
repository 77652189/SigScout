# ADR-006：实验引导探索是候选压缩而非产量模型

## 背景

现有实验数据规模小、批次条件不同，并包含短信号肽与完整 leader。直接训练或生成统一产量分会制造超出证据范围的精度感，但实验结果仍可用于缩小下一轮探索空间。

## 决策

只使用批内归一化表现，并在相同实验单元类型内建立锚点。探索面板显式组合正向邻域、通用支持、多样性和低表现邻域对照，输出渠道与选择理由；不声称跨批次绝对比较、统计显著性或产量预测。

## 后果

面板大小和渠道配额可以调整，排序结果必须被解释为实验设计启发式。扩展为产量模型需要新的数据规模、验证方案和单独授权。

## 替代关系

无。
