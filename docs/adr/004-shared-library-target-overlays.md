# ADR-004：共享候选库与目标覆盖层分离

## 背景

宿主来源信号肽可以服务多个目标蛋白。为每个目标复制候选库会造成证据分叉，但目标序列、实验反馈和外部定位结果又不能跨目标复用。

## 决策

候选库保持目标无关。目标专属实验候选、实验反馈、融合构建、定位缓存、选择状态和导出作为按目标隔离的覆盖层；没有数据的目标显示空态，不借用其他目标的结果。

## 后果

刷新共享候选不会复制目标库；新增目标只需建立自己的构建和本地数据边界。目标切换、缓存键和会话状态必须携带目标标识。

## 替代关系

无。
