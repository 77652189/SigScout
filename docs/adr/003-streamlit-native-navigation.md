# ADR-003：使用原生导航并避开保留页面目录

## 背景

手写导航增加入口耦合；名为 `pages` 的目录会被 Streamlit 自动发现，导致不应独立运行的模块出现在导航中。

## 决策

应用入口使用 `st.navigation` 和 `st.Page`；页面渲染模块存放在 `ui/views`。

## 后果

导航由入口集中声明，页面适配函数保持无参；新增 UI 子目录不得命名为 `pages`。

## 替代关系

无。
