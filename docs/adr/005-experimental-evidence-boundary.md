# ADR-005：实验反馈按精确序列关联并与定位评分独立

## 背景

实验可能验证完整融合构建，也可能只与候选 A 段相关。按名称或相似序列传播会夸大证据；把实验表现与外部定位概率合成总分则会掩盖两者不同的科学含义。

## 决策

实验反馈只按氨基酸序列精确关联。完整融合构建一致时才标记为构建级证据；仅 A 段一致时只显示相关实验。实验字段在展示和导出时与定位结果并列合并，不改变外部定位分、加工风险分或原始缓存。

## 后果

修改 B、C 或构建类型会使证据降级；相似候选不会自动继承实验结论。用户需要分别解读定位预测和实验事实，但证据边界保持可审查。

## 替代关系

无。
